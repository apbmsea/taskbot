# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ

## Проблема:
В переменной `PUBLIC_URL` на Render есть лишний слэш в конце: `https://taskbot-nhkb.onrender.com/`

Это создает двойные слэши: `//telegram` и `//webhook`

## Исправление на Render:

1. Зайдите в **Environment** на Render
2. Найдите `PUBLIC_URL`
3. Измените с `https://taskbot-nhkb.onrender.com/` на `https://taskbot-nhkb.onrender.com`
4. **Уберите слэш в конце!**
5. Нажмите **Save Changes**

## После исправления:

Запустите локально для переустановки webhook:
```bash
node check-webhook.js
```

Или просто подождите пока Render перезапустит сервис.

## Проверка:

В логах должно быть:
```
📡 Webhook URL: https://taskbot-nhkb.onrender.com/webhook
✅ Telegram webhook установлен: https://taskbot-nhkb.onrender.com/telegram
```

**БЕЗ двойных слэшей!**
